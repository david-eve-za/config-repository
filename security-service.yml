server:
  port: 8081
spring:
  application:
    name: security-service
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,info
  tracing:
    enabled: true
    sampling:
      probability: 1.0
# Phase 2 will add: oauth2 / authorization-server settings here